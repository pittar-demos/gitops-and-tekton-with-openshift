apiVersion: v1
kind: ResourceQuota
metadata:
  name: large-cpu-memory-quota
spec:
  hard:
    requests.cpu: '2'
    requests.memory: 6Gi
    limits.cpu: '8'
    limits.memory: 6Gi
  scopes:
    - NotTerminating