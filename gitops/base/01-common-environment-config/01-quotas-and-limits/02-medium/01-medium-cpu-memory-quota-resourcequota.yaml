apiVersion: v1
kind: ResourceQuota
metadata:
  name: medium-cpu-memory-quota
spec:
  hard:
    requests.cpu: '2'
    requests.memory: 4Gi
    limits.cpu: '4'
    limits.memory: 6Gi
  scopes:
    - NotTerminating