apiVersion: v1
kind: ResourceQuota
metadata:
  name: medium-cpu-memory-quota
spec:
  hard:
    requests.cpu: '1'
    requests.memory: 2Gi
    limits.cpu: '3'
    limits.memory: 2Gi
  scopes:
    - NotTerminating