apiVersion: v1
kind: ResourceQuota
metadata:
  name: default-cpu-memory-quota
spec:
  hard:
    requests.cpu: '2'
    requests.memory: 4Gi
    limits.cpu: '4'
    limits.memory: 4Gi
  scopes:
    - NotTerminating